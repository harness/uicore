# Button

import ReactLive from '../shared/ReactLive'

```js
import { Button } from 'ui-platform'
```

export const code = `
<>
<Heading level="1">Deployments</Heading>
<Button text="Let go" icon="refresh" onClick={() => alert('Hello World')}></Button>
</>
`

<ReactLive>{code}</ReactLive>
