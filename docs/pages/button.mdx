# Button

To use Button, first you need to import it.

# Overview

Soma's writing on how it should behave.

```js
import { Button } from 'ui-platform'
```

### Playground

```js live=true
<Button text="Let go" icon="refresh" onClick={() => alert('Hello World')}></Button>
```
