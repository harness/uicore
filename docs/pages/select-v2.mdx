# Select V2 --- docs/pages/select-v2.mdx

import { SimpleSelect, AsyncSelect} from '../shared/SelectV2'

## Import

```js
import { SelectNG } from '@wings-software/uikit'
```

```js
interface SelectOption {
  label: string
  value: string | number | symbol
  icon?: IconProps
}

```


## Simple Select


<SimpleSelect />

```js
<Layout.Horizontal id="primary-borderless-buttons">
    <SelectNG
    items={[]}
    value={val}
    onChange={setVal}
    itemRenderer={(item, props) => (
        <Button style={{ width: '100%', display: 'block' }} minimal onClick={props.handleClick}>
        {item.label}
        </Button>
    )}>
    <Button
        intent="primary"
        minimal
        style={{ border: '1px solid var(--grey-400)', width: '100%', display: 'block' }}
        icon={val && val.icon && val.icon.name}
        rightIcon="chevron-down"
        text={val ? val.label : 'Search...'}
    />
    </SelectNG>
</Layout.Horizontal>
```


## Async Select

<AsyncSelect />

```js
<Layout.Horizontal id="primary-borderless-buttons">
    <SelectNG
    items={dummyPromise}
    value={val}
    onChange={setVal}
    itemRenderer={(item, props) => (
        <Button style={{ width: '100%', display: 'block' }} minimal onClick={props.handleClick}>
        {item.label}
        </Button>
    )}>
    <Button
        intent="primary"
        minimal
        style={{ border: '1px solid var(--grey-400)', width: '100%', display: 'block' }}
        icon={val && val.icon && val.icon.name}
        rightIcon="chevron-down"
        text={val ? val.label : 'Search...'}
    />
    </SelectNG>
</Layout.Horizontal>
```
