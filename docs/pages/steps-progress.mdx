# Steps Progress --- docs/pages/steps-progress.mdx

```js live=true 
<StepsProgress 
    steps={[
        'Checking Delegate', 
        'Establising Connection with the Delegate and Kubernetes', 
        'Checking for heartbeat', 
        'Verifying Connection'
        ]}
    intent={Intent.SUCCESS}
    current={1}
    currentStatus={'PROCESS'}
/>
```

```js live=true 
<StepsProgress 
    steps={[
        'Checking Delegate', 
        'Establising Connection with the Delegate and Kubernetes', 
        'Checking for heartbeat', 
        'Verifying Connection'
        ]}
    intent={Intent.DANGER}
    current={3}
    currentStatus={'ERROR'}
/>
```


```js live=true 
<StepsProgress 
    steps={[
        'Checking Delegate', 
        'Establising Connection with the Delegate and Kubernetes', 
        'Checking for heartbeat', 
        'Verifying Connection'
        ]}
    intent={Intent.SUCCESS}
    current={4}
    currentStatus={'DONE'}
/>
```