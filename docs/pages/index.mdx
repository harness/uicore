import { useState } from 'react'
import { copy } from '../static'
import { Button, Popover, Position, Tooltip } from '@blueprintjs/core'

<Popover content={<h1>Popover!</h1>} position={Position.RIGHT} usePortal={false}>
  <Button onClick={() => copy('Hey you!!!!')}>Hover and click me</Button>
</Popover>

# Overview

```js
import React from 'react'
import Example from './place'

export default () => <Example />
```

```js render=true
<div style={{ backgroundColor: 'rebeccapurple', padding: '20px', marginTop: '20px' }} />
```

```js live=true
<div style={{ backgroundColor: 'tomato', padding: '20px' }} />
```

export const HelloWorld = () => {
  const [count, setCount] = useState(0)
  return <h1 onClick={() => setCount(count + 1)}>Hello World: {count}</h1>
}

<HelloWorld />
