import React from 'react';
interface Props {
    inline?: boolean;
    children: React.ReactNode;
}
export default function Text({ inline, children }: Props): JSX.Element;
export declare function test(): JSX.Element;
export {};
