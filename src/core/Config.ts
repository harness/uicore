// import * as mentions from '@wings-software/mentions'

export const Config = {
  DISABLE_LINK_REWRITE: false
}

// mentions.setDefaults({
//   identifiersSet: /[A-Za-z0-9_.'"\(\)]/, // eslint-disable-line no-useless-escape
//   trigger: ['$', '${'],
//   rule: '${__match__}',
//   cached: true
// })
